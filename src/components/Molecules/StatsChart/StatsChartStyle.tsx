import { StyleSheet } from "react-native";
import { hp } from "../../../utils/helper";

const StatsChartStyle = StyleSheet.create({
  graphStyle: {
    marginVertical: 10,
    borderRadius: 16,
    marginTop : hp(1)
  },
});
export default StatsChartStyle;
